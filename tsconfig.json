{
  "compilerOptions": {
    "outDir": "./dist",        // Directorio de salida para el código compilado
    "rootDir": "./src",        // Directorio donde están los archivos TypeScript originales
    "target": "es2017",        // Cambiar de "es5" a "es2017" para que soporte async/await
    "module": "commonjs",      // Esto puede seguir siendo commonjs si estás usando Node.js
    "strict": true             // Esto activa la verificación estricta de tipos
  },
  "include": ["src/**/*.ts"],  // Incluir todos los archivos .ts en la carpeta src
  "exclude": ["node_modules"]  // Excluir node_modules
}
